import ReviewSection from './review_section';

const VenueSidebar = () => {
    return (
        <ReviewSection />
    );
};

VenueSidebar.ReviewSection = ReviewSection;

export default VenueSidebar;
