import * as React from 'react';
// import Footer from '@src/components/concrete/Footer';

export type Props = {};

export default (_: React.PropsWithChildren<Props>) => {
  return (
    <div>Footer</div>
  );
};
