import { StyleSheet } from 'aphrodite/no-important';

export default StyleSheet.create({
  itemWrapper: {
    display: 'flex',
  },
  itemContainer: {
    display: 'inline-flex',
    alignSelf: 'center',
  },
});
