import { StyleSheet } from 'aphrodite/no-important';

export default StyleSheet.create({
  buttonContainer: {
    color: '#ffffff',
  },
  buttonInner: {
    display: 'flex',
    alignItems: 'center',
  },
});
