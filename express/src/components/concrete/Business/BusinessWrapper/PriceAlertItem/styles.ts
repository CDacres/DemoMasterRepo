import { StyleSheet } from 'aphrodite/no-important';

export default StyleSheet.create({
  wrapper: {
    marginTop: '20px',
  },
  buttonWithoutLocation: {
    display: 'inline-block',
    verticalAlign: 'top',
  },
});
