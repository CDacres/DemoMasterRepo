import { StyleSheet } from 'aphrodite/no-important';

export default StyleSheet.create({
  wrapper: {
    minWidth: '0px',
    width: '100%',
  },
  container: {
    display: 'flex',
    flexWrap: 'wrap',
  },
});
