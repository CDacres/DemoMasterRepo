import { StyleSheet } from 'aphrodite/no-important';

export default StyleSheet.create({
  mapToggleWrapper: {
    flexShrink: 0,
    flexGrow: 0,
    display: 'flex',
    alignItems: 'center',
  },
  mapToggleTableCell: {
    fontSize: '14px',
  },
});
