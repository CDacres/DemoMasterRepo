import { StyleSheet } from 'aphrodite/no-important';

export default StyleSheet.create({
  filterBarItemContainer: {
    alignItems: 'center',
    display: 'flex',
    position: 'relative',
    width: '100%',
    zIndex: 4,
    height: '48px',
    background: '#ffffff',
    borderBottom: '0px',
  },
});
