// import { ofType } from 'redux-observable';
// import { of as rxOf } from 'rxjs';
// import { switchMap } from 'rxjs/operators';

// Actions
// import { SET_VERTICAL_SUCCESS } from '@src/store/modules/search/verticals';

// import { toggleMap } from '@src/store/modules/pages/search/map';

// export default action$ => action$.pipe(
//   ofType(SET_VERTICAL_SUCCESS),
//   switchMap(({ vertical: { mapIsVisible } }) => {
//     return rxOf(toggleMap(mapIsVisible));
//   })
// );

export default (() => null);
